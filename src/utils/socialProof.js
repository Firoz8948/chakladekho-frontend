/**
 * Intentionally disabled: do not invent ratings or review counts for Merchant Center trust.
 * Wire this only when real review data exists.
 */
export function getProductSocialProof() {
  return null;
}
